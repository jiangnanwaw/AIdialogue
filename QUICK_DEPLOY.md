# 快速部署指南 - 5 分钟上线

## 📦 上传到 GitHub 的文件清单

### ✅ 必须上传（7 个文件）

```
您的项目/
├── api/
│   └── chat.js              ✅ 上传
├── lib/
│   └── db.js                ✅ 上传
├── public/
│   └── index.html           ✅ 上传（Vercel 版本）
├── vercel.json              ✅ 上传
├── package.json             ✅ 上传（已更新）
├── .gitignore               ✅ 上传（已更新）
└── README.md                ✅ 可选
```

---

## 🚀 三步部署

### 第 1 步：推送到 GitHub

```bash
# 在项目目录执行
cd C:\Users\Administrator\Desktop\Supabase

# 如果已有 Git 仓库，直接提交
git add .
git commit -m "准备 Vercel 部署"
git push

# 如果没有，先初始化
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/你的用户名/ai-chat-vercel.git
git push -u origin main
```

### 第 2 步：在 Vercel 导入

1. 打开 https://vercel.com
2. 点击 **Add New** → **Project**
3. 选择您的 GitHub 仓库
4. 点击 **Import**

### 第 3 步：配置环境变量

在 Configure Project 页面添加：

| Name | Value |
|------|-------|
| `DEEPSEEK_API_KEY` | `sk-9a6e2beae112468dba3d212df48354f0` |
| `DB_USER` | `csfh` |
| `DB_PASSWORD` | `fh123456` |
| `DB_SERVER` | `csfhcdz.f3322.net` |
| `DB_DATABASE` | `chargingdata` |

点击 **Deploy**

---

## 🎉 完成！

等待 1-2 分钟，访问您的网站：

```
https://ai-chat-vercel.vercel.app
```

---

## 🔧 Vercel 操作界面说明

### 部署配置页面

```
┌─────────────────────────────────────────────────────┐
│  Configure Project                                  │
│                                                     │
│  Project Name: ai-chat-vercel                       │
│  Framework Preset: Other                            │
│  Root Directory: ./                                 │
│                                                     │
│  Environment Variables:                             │
│  ┌─────────────────────────────────────────────┐   │
│  │ Name              Value                     │   │
│  │ DEEPSEEK_API_KEY   sk-xxx...                │   │
│  │ DB_USER           csfh                      │   │
│  │ DB_PASSWORD       fh123456                  │   │
│  │ DB_SERVER         csfhcdz.f3322.net          │   │
│  │ DB_DATABASE       chargingdata              │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  [ Deploy ]                                         │
└─────────────────────────────────────────────────────┘
```

### 项目 Dashboard

```
┌─────────────────────────────────────────────────────┐
│  ai-chat-vercel                                    │
│                                                     │
│  Deployments  (3)                                   │
│  ┌─────────────────────────────────────────────┐   │
│  │ ✅ Ready  [ai-chat-vercel.vercel.app]       │   │
│  │    2 minutes ago                             │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  Settings  Functions  Domains  Logs                │
└─────────────────────────────────────────────────────┘
```

---

## 📝 更新代码后

只需 `git push`，Vercel 自动部署：

```bash
git add .
git commit -m "更新功能"
git push
```

---

## ❓ 遇到问题？

1. **部署失败** → 查看 Deployments → 点击部署 → 查看日志
2. **API 报错** → 检查环境变量是否配置正确
3. **数据库连接失败** → 确认服务器地址和密码正确

详细教程请查看 `VERCEL_COMPLETE_GUIDE.md`
